query allPhotosWithTag($input: AllPhotosWithTagInput!) {
  allPhotosWithTag(input: $input) {
    tagInfo {
      id
      name
      description
      coverImage {
        ...ImageInfo
      }
    }
    total
    photos {
      ...PhotoInfo
    }
  }
}
